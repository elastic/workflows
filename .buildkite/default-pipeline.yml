---
# $yaml-language-server: $schema=https://raw.githubusercontent.com/buildkite/pipeline-schema/main/schema.json
steps:
  - label: "Build"
    command: "true"

  # PR validation (build:catalog, lint, workflow YAML validation) runs in
  # GitHub Actions (.github/workflows/validate.yml) where fork PRs are
  # supported. This step is a placeholder; a post-merge backstop run can be
  # added here later if needed.
  - label: "Validate workflows"
    command: "true"

  - label: "Deploy"
    command: "true"

  - label: "Publish"
    command: "true"
